# Database (auto-configured by pnpm setup with Docker)
DATABASE_URL=postgresql://siteseer:siteseer@localhost:5432/siteseer

# Better Auth
# Required for local pnpm dev and as Docker Compose secret source for the app profile
BETTER_AUTH_SECRET=change-me-to-a-random-32-char-secret
# For local pnpm dev (Docker app service overrides this to http://localhost:3000)
BETTER_AUTH_URL=http://localhost:5173

# OpenTelemetry (required by src/instrumentation.server.ts)
# For local pnpm dev with Jaeger via compose: http://localhost:4318/v1/traces
# Docker app service overrides this to http://jaeger:4318/v1/traces
OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=http://localhost:4318/v1/traces

# Optional Docker app-service overrides (used by compose `app` profile)
# DATABASE_URL_DOCKER=postgresql://siteseer:siteseer@postgres:5432/siteseer
# BETTER_AUTH_URL_DOCKER=http://localhost:3000
# OTEL_EXPORTER_OTLP_TRACES_ENDPOINT_DOCKER=http://jaeger:4318/v1/traces

# Environment
NODE_ENV=development
DEMO=false
